version: '3'
services:
    postgres:
        image: openbmp/postgres:2.2.1
        restart: unless-stopped
        deploy:
            restart_policy:
                condition: any
            placement:
                constraints:
                    - node.role == manager
        healthcheck:
            test: ["CMD-SHELL", "pg_isready -U openbmp"]
            interval: 30s
            timeout: 10s
            retries: 5
        privileged: true
        shm_size: 1536m
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - postgres_data:/var/lib/postgresql/data
            - timescale_data:/var/lib/postgresql/ts
        command: >
            -c max_wal_size=10GB
        environment:
            POSTGRES_PASSWORD: openbmp
            POSTGRES_USER: openbmp
            POSTGRES_DB: openbmp
        
    web:
        image: bgpdata/bgpdata:0.1.13
        restart: unless-stopped
        deploy:
            restart_policy:
                condition: any
            placement:
                constraints:
                    - node.role == manager
        healthcheck:
            test: ["CMD", "curl", "-f", "http://localhost:8080"]
            interval: 30s
            timeout: 10s
            retries: 3
            start_period: 40s
        environment:
            SECRET_KEY: ${SECRET_KEY}
            POSTGRES_HOST: postgres
            POSTGRES_PORT: 5432
            POSTGRES_DB: openbmp
            POSTGRES_USER: openbmp
            POSTGRES_PASSWORD: openbmp
            POSTMARK_API_KEY: ${POSTMARK_API_KEY}
            FLASK_HOST: http://localhost:8080
            ENVIRONMENT: production
        depends_on:
            - postgres
        volumes:
            - web_data:/var/log/migrations
        entrypoint: >
            /bin/sh -c "python manage.py migrate && python manage.py run --host 0.0.0.0"
    relay-rrc00:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc00
            OPENBMP_CONNECT: collector-rrc00:5000
        depends_on:
            - collector-rrc00
        volumes:
            - relay_rrc00_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((0 * 120)) && python manage.py relay"
    
    relay-rrc01:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc01
            OPENBMP_CONNECT: collector-rrc01:5000
        depends_on:
            - collector-rrc01
        volumes:
            - relay_rrc01_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((1 * 120)) && python manage.py relay"
    
    relay-rrc03:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc03
            OPENBMP_CONNECT: collector-rrc03:5000
        depends_on:
            - collector-rrc03
        volumes:
            - relay_rrc03_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((2 * 120)) && python manage.py relay"
    
    relay-rrc04:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc04
            OPENBMP_CONNECT: collector-rrc04:5000
        depends_on:
            - collector-rrc04
        volumes:
            - relay_rrc04_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((3 * 120)) && python manage.py relay"
    
    relay-rrc05:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc05
            OPENBMP_CONNECT: collector-rrc05:5000
        depends_on:
            - collector-rrc05
        volumes:
            - relay_rrc05_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((4 * 120)) && python manage.py relay"
    
    relay-rrc06:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc06
            OPENBMP_CONNECT: collector-rrc06:5000
        depends_on:
            - collector-rrc06
        volumes:
            - relay_rrc06_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((5 * 120)) && python manage.py relay"
    
    relay-rrc07:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc07
            OPENBMP_CONNECT: collector-rrc07:5000
        depends_on:
            - collector-rrc07
        volumes:
            - relay_rrc07_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((6 * 120)) && python manage.py relay"
    
    relay-rrc10:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc10
            OPENBMP_CONNECT: collector-rrc10:5000
        depends_on:
            - collector-rrc10
        volumes:
            - relay_rrc10_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((7 * 120)) && python manage.py relay"
    
    relay-rrc11:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc11
            OPENBMP_CONNECT: collector-rrc11:5000
        depends_on:
            - collector-rrc11
        volumes:
            - relay_rrc11_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((8 * 120)) && python manage.py relay"
    
    relay-rrc12:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc12
            OPENBMP_CONNECT: collector-rrc12:5000
        depends_on:
            - collector-rrc12
        volumes:
            - relay_rrc12_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((9 * 120)) && python manage.py relay"
    
    relay-rrc13:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc13
            OPENBMP_CONNECT: collector-rrc13:5000
        depends_on:
            - collector-rrc13
        volumes:
            - relay_rrc13_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((10 * 120)) && python manage.py relay"
    
    relay-rrc14:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc14
            OPENBMP_CONNECT: collector-rrc14:5000
        depends_on:
            - collector-rrc14
        volumes:
            - relay_rrc14_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((11 * 120)) && python manage.py relay"
    
    relay-rrc15:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc15
            OPENBMP_CONNECT: collector-rrc15:5000
        depends_on:
            - collector-rrc15
        volumes:
            - relay_rrc15_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((12 * 120)) && python manage.py relay"
    
    relay-rrc16:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc16
            OPENBMP_CONNECT: collector-rrc16:5000
        depends_on:
            - collector-rrc16
        volumes:
            - relay_rrc16_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((13 * 120)) && python manage.py relay"
    
    relay-rrc18:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc18
            OPENBMP_CONNECT: collector-rrc18:5000
        depends_on:
            - collector-rrc18
        volumes:
            - relay_rrc18_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((14 * 120)) && python manage.py relay"
    
    relay-rrc19:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc19
            OPENBMP_CONNECT: collector-rrc19:5000
        depends_on:
            - collector-rrc19
        volumes:
            - relay_rrc19_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((15 * 120)) && python manage.py relay"
    
    relay-rrc20:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc20
            OPENBMP_CONNECT: collector-rrc20:5000
        depends_on:
            - collector-rrc20
        volumes:
            - relay_rrc20_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((16 * 120)) && python manage.py relay"
    
    relay-rrc21:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc21
            OPENBMP_CONNECT: collector-rrc21:5000
        depends_on:
            - collector-rrc21
        volumes:
            - relay_rrc21_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((17 * 120)) && python manage.py relay"
    
    relay-rrc22:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc22
            OPENBMP_CONNECT: collector-rrc22:5000
        depends_on:
            - collector-rrc22
        volumes:
            - relay_rrc22_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((18 * 120)) && python manage.py relay"
    
    relay-rrc23:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc23
            OPENBMP_CONNECT: collector-rrc23:5000
        depends_on:
            - collector-rrc23
        volumes:
            - relay_rrc23_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((19 * 120)) && python manage.py relay"
    
    relay-rrc24:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc24
            OPENBMP_CONNECT: collector-rrc24:5000
        depends_on:
            - collector-rrc24
        volumes:
            - relay_rrc24_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((20 * 120)) && python manage.py relay"
    
    relay-rrc25:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc25
            OPENBMP_CONNECT: collector-rrc25:5000
        depends_on:
            - collector-rrc25
        volumes:
            - relay_rrc25_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((21 * 120)) && python manage.py relay"
    
    relay-rrc26:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rrc26
            OPENBMP_CONNECT: collector-rrc26:5000
        depends_on:
            - collector-rrc26
        volumes:
            - relay_rrc26_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((22 * 120)) && python manage.py relay"
    
    relay-route-views2:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: route-views2
            OPENBMP_CONNECT: collector-route-views2:5000
        depends_on:
            - collector-route-views2
        volumes:
            - relay_route_views2_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((23 * 120)) && python manage.py relay"
    
    relay-route-views3:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: route-views3
            OPENBMP_CONNECT: collector-route-views3:5000
        depends_on:
            - collector-route-views3
        volumes:
            - relay_route_views3_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((24 * 120)) && python manage.py relay"
    
    relay-route-views4:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: route-views4
            OPENBMP_CONNECT: collector-route-views4:5000
        depends_on:
            - collector-route-views4
        volumes:
            - relay_route_views4_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((25 * 120)) && python manage.py relay"
    
    relay-route-views5:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: route-views5
            OPENBMP_CONNECT: collector-route-views5:5000
        depends_on:
            - collector-route-views5
        volumes:
            - relay_route_views5_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((26 * 120)) && python manage.py relay"
    
    relay-route-views6:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: route-views6
            OPENBMP_CONNECT: collector-route-views6:5000
        depends_on:
            - collector-route-views6
        volumes:
            - relay_route_views6_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((27 * 120)) && python manage.py relay"
    
    relay-route-views7:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: route-views7
            OPENBMP_CONNECT: collector-route-views7:5000
        depends_on:
            - collector-route-views7
        volumes:
            - relay_route_views7_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((28 * 120)) && python manage.py relay"
    
    relay-route-views8:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: route-views8
            OPENBMP_CONNECT: collector-route-views8:5000
        depends_on:
            - collector-route-views8
        volumes:
            - relay_route_views8_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((29 * 120)) && python manage.py relay"
    
    relay-amsix:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: amsix
            OPENBMP_CONNECT: collector-amsix:5000
        depends_on:
            - collector-amsix
        volumes:
            - relay_amsix_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((30 * 120)) && python manage.py relay"
    
    relay-amsix-ams:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: amsix.ams
            OPENBMP_CONNECT: collector-amsix-ams:5000
        depends_on:
            - collector-amsix-ams
        volumes:
            - relay_amsix_ams_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((31 * 120)) && python manage.py relay"
    
    relay-cix-atl:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: cix.atl
            OPENBMP_CONNECT: collector-cix-atl:5000
        depends_on:
            - collector-cix-atl
        volumes:
            - relay_cix_atl_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((32 * 120)) && python manage.py relay"
    
    relay-bdix:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: bdix
            OPENBMP_CONNECT: collector-bdix:5000
        depends_on:
            - collector-bdix
        volumes:
            - relay_bdix_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((33 * 120)) && python manage.py relay"
    
    relay-bknix:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: bknix
            OPENBMP_CONNECT: collector-bknix:5000
        depends_on:
            - collector-bknix
        volumes:
            - relay_bknix_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((34 * 120)) && python manage.py relay"
    
    relay-chicago:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: chicago
            OPENBMP_CONNECT: collector-chicago:5000
        depends_on:
            - collector-chicago
        volumes:
            - relay_chicago_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((35 * 120)) && python manage.py relay"
    
    relay-chile:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: chile
            OPENBMP_CONNECT: collector-chile:5000
        depends_on:
            - collector-chile
        volumes:
            - relay_chile_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((36 * 120)) && python manage.py relay"
    
    relay-eqix:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: eqix
            OPENBMP_CONNECT: collector-eqix:5000
        depends_on:
            - collector-eqix
        volumes:
            - relay_eqix_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((37 * 120)) && python manage.py relay"
    
    relay-flix:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: flix
            OPENBMP_CONNECT: collector-flix:5000
        depends_on:
            - collector-flix
        volumes:
            - relay_flix_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((38 * 120)) && python manage.py relay"
    
    relay-gixa:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: gixa
            OPENBMP_CONNECT: collector-gixa:5000
        depends_on:
            - collector-gixa
        volumes:
            - relay_gixa_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((39 * 120)) && python manage.py relay"
    
    relay-gorex:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: gorex
            OPENBMP_CONNECT: collector-gorex:5000
        depends_on:
            - collector-gorex
        volumes:
            - relay_gorex_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((40 * 120)) && python manage.py relay"
    
    relay-interlan-otp:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: interlan.otp
            OPENBMP_CONNECT: collector-interlan-otp:5000
        depends_on:
            - collector-interlan-otp
        volumes:
            - relay_interlan_otp_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((41 * 120)) && python manage.py relay"
    
    relay-iraq-ixp-bgw:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: iraq-ixp.bgw
            OPENBMP_CONNECT: collector-iraq-ixp-bgw:5000
        depends_on:
            - collector-iraq-ixp-bgw
        volumes:
            - relay_iraq_ixp_bgw_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((42 * 120)) && python manage.py relay"
    
    relay-isc:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: isc
            OPENBMP_CONNECT: collector-isc:5000
        depends_on:
            - collector-isc
        volumes:
            - relay_isc_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((43 * 120)) && python manage.py relay"
    
    relay-kinx-icn:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: kinx.icn
            OPENBMP_CONNECT: collector-kinx-icn:5000
        depends_on:
            - collector-kinx-icn
        volumes:
            - relay_kinx_icn_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((44 * 120)) && python manage.py relay"
    
    relay-namex-fco:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: namex.fco
            OPENBMP_CONNECT: collector-namex-fco:5000
        depends_on:
            - collector-namex-fco
        volumes:
            - relay_namex_fco_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((45 * 120)) && python manage.py relay"
    
    relay-kixp:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: kixp
            OPENBMP_CONNECT: collector-kixp:5000
        depends_on:
            - collector-kixp
        volumes:
            - relay_kixp_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((46 * 120)) && python manage.py relay"
    
    relay-jinx:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: jinx
            OPENBMP_CONNECT: collector-jinx:5000
        depends_on:
            - collector-jinx
        volumes:
            - relay_jinx_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((47 * 120)) && python manage.py relay"
    
    relay-linx:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: linx
            OPENBMP_CONNECT: collector-linx:5000
        depends_on:
            - collector-linx
        volumes:
            - relay_linx_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((48 * 120)) && python manage.py relay"
    
    relay-mwix:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: mwix
            OPENBMP_CONNECT: collector-mwix:5000
        depends_on:
            - collector-mwix
        volumes:
            - relay_mwix_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((49 * 120)) && python manage.py relay"
    
    relay-napafrica:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: napafrica
            OPENBMP_CONNECT: collector-napafrica:5000
        depends_on:
            - collector-napafrica
        volumes:
            - relay_napafrica_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((50 * 120)) && python manage.py relay"
    
    relay-nwax:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: nwax
            OPENBMP_CONNECT: collector-nwax:5000
        depends_on:
            - collector-nwax
        volumes:
            - relay_nwax_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((51 * 120)) && python manage.py relay"
    
    relay-pacwave-lax:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: pacwave.lax
            OPENBMP_CONNECT: collector-pacwave-lax:5000
        depends_on:
            - collector-pacwave-lax
        volumes:
            - relay_pacwave_lax_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((52 * 120)) && python manage.py relay"
    
    relay-pit-scl:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: pit.scl
            OPENBMP_CONNECT: collector-pit-scl:5000
        depends_on:
            - collector-pit-scl
        volumes:
            - relay_pit_scl_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((53 * 120)) && python manage.py relay"
    
    relay-pitmx-qro:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: pitmx.qro
            OPENBMP_CONNECT: collector-pitmx-qro:5000
        depends_on:
            - collector-pitmx-qro
        volumes:
            - relay_pitmx_qro_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((54 * 120)) && python manage.py relay"
    
    relay-phiox:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: phiox
            OPENBMP_CONNECT: collector-phiox:5000
        depends_on:
            - collector-phiox
        volumes:
            - relay_phiox_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((55 * 120)) && python manage.py relay"
    
    relay-telxatl:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: telxatl
            OPENBMP_CONNECT: collector-telxatl:5000
        depends_on:
            - collector-telxatl
        volumes:
            - relay_telxatl_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((56 * 120)) && python manage.py relay"
    
    relay-wide:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: wide
            OPENBMP_CONNECT: collector-wide:5000
        depends_on:
            - collector-wide
        volumes:
            - relay_wide_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((57 * 120)) && python manage.py relay"
    
    relay-sydney:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: sydney
            OPENBMP_CONNECT: collector-sydney:5000
        depends_on:
            - collector-sydney
        volumes:
            - relay_sydney_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((58 * 120)) && python manage.py relay"
    
    relay-saopaulo:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: saopaulo
            OPENBMP_CONNECT: collector-saopaulo:5000
        depends_on:
            - collector-saopaulo
        volumes:
            - relay_saopaulo_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((59 * 120)) && python manage.py relay"
    
    relay-route-views2-saopaulo:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: route-views2.saopaulo
            OPENBMP_CONNECT: collector-route-views2-saopaulo:5000
        depends_on:
            - collector-route-views2-saopaulo
        volumes:
            - relay_route_views2_saopaulo_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((60 * 120)) && python manage.py relay"
    
    relay-sg:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: sg
            OPENBMP_CONNECT: collector-sg:5000
        depends_on:
            - collector-sg
        volumes:
            - relay_sg_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((61 * 120)) && python manage.py relay"
    
    relay-perth:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: perth
            OPENBMP_CONNECT: collector-perth:5000
        depends_on:
            - collector-perth
        volumes:
            - relay_perth_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((62 * 120)) && python manage.py relay"
    
    relay-peru:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: peru
            OPENBMP_CONNECT: collector-peru:5000
        depends_on:
            - collector-peru
        volumes:
            - relay_peru_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((63 * 120)) && python manage.py relay"
    
    relay-sfmix:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: sfmix
            OPENBMP_CONNECT: collector-sfmix:5000
        depends_on:
            - collector-sfmix
        volumes:
            - relay_sfmix_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((64 * 120)) && python manage.py relay"
    
    relay-siex:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: siex
            OPENBMP_CONNECT: collector-siex:5000
        depends_on:
            - collector-siex
        volumes:
            - relay_siex_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((65 * 120)) && python manage.py relay"
    
    relay-soxrs:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: soxrs
            OPENBMP_CONNECT: collector-soxrs:5000
        depends_on:
            - collector-soxrs
        volumes:
            - relay_soxrs_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((66 * 120)) && python manage.py relay"
    
    relay-rio:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: rio
            OPENBMP_CONNECT: collector-rio:5000
        depends_on:
            - collector-rio
        volumes:
            - relay_rio_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((67 * 120)) && python manage.py relay"
    
    relay-fortaleza:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: fortaleza
            OPENBMP_CONNECT: collector-fortaleza:5000
        depends_on:
            - collector-fortaleza
        volumes:
            - relay_fortaleza_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((68 * 120)) && python manage.py relay"
    
    relay-uaeix:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: uaeix
            OPENBMP_CONNECT: collector-uaeix:5000
        depends_on:
            - collector-uaeix
        volumes:
            - relay_uaeix_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((69 * 120)) && python manage.py relay"
    
    relay-ny:
        image: bgpdata/bgpdata:0.1.13
        restart: "no"
        stop_signal: SIGTERM                    # Wind down relay safely before stopping
        stop_grace_period: 20s                  # Wait for 20 seconds before forcefully stopping
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == manager
        environment:
            HOST: ny
            OPENBMP_CONNECT: collector-ny:5000
        depends_on:
            - collector-ny
        volumes:
            - relay_ny_data:/var/lib/rocksdb
        entrypoint: >
            bash -c "sleep $$((70 * 120)) && python manage.py relay"
    
    collector-rrc00:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc01:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc03:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc04:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc05:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc06:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc07:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc10:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc11:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc12:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc13:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc14:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc15:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc16:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc18:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc19:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc20:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc21:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc22:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc23:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc24:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc25:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rrc26:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-route-views2:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-route-views3:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-route-views4:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-route-views5:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-route-views6:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-route-views7:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-route-views8:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-amsix:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-amsix-ams:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-cix-atl:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-bdix:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-bknix:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-chicago:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-chile:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-eqix:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-flix:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-gixa:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-gorex:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-interlan-otp:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-iraq-ixp-bgw:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-isc:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-kinx-icn:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-namex-fco:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-kixp:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-jinx:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-linx:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-mwix:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-napafrica:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-nwax:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-pacwave-lax:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-pit-scl:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-pitmx-qro:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-phiox:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-telxatl:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-wide:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-sydney:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-saopaulo:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-route-views2-saopaulo:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-sg:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-perth:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-peru:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-sfmix:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-siex:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-soxrs:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-rio:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-fortaleza:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-uaeix:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    
    collector-ny:
        image: openbmp/collector:2.2.3
        restart: "no"
        deploy:
            restart_policy:
                condition: none
            placement:
                constraints:
                    - node.role == worker
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/5000"]
            interval: 30s
            timeout: 10s
            retries: 3
        depends_on:
            - postgres
            - kafka
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        environment:
            KAFKA_FQDN: kafka:29092
    

    openbmp:
        image: openbmp/psql-app:2.2.2
        restart: unless-stopped
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        deploy:
            restart_policy:
                condition: any
            placement:
                constraints:
                    - node.role == worker
        depends_on:
            - postgres
            - kafka
        volumes:
            - openbmp_data:/config
        environment:
            MEM: 3                                           # Set memory to at least 2GB but ideally 4GB
            KAFKA_FQDN: kafka:29092
            RPKI_URL: https://rpki.cloudflare.com/rpki.json  # define the URL to retrieve json endoed RPKI data
            RPKI_PASS: None
            RPKI_USER: None
            ENABLE_RPKI: 1                                   # 1 enables, 0 disables RPKI sync
            ENABLE_IRR: 1                                    # 1 enables, 0 disables IRR sync
            ENABLE_DBIP: 1                                   # 1 enables, 0 disables DBIP import
            POSTGRES_REPORT_WINDOW: '8 minute'               # default POSTGRESS window to select when building
                                                             #   summary tables. For deployments that absorb large
                                                             #   bursts increase the value, ex 60 minute
            POSTGRES_HOST: postgres
            POSTGRES_PORT: 5432
            POSTGRES_DB: openbmp
            POSTGRES_USER: openbmp
            POSTGRES_PASSWORD: openbmp
            POSTGRES_DROP_peer_event_log: '1 month'
            POSTGRES_DROP_stat_reports: '1 day'
            POSTGRES_DROP_ip_rib_log: '1 day'
            POSTGRES_DROP_alerts: '1 day'
            POSTGRES_DROP_ls_nodes_log: '2 days'
            POSTGRES_DROP_ls_links_log: '2 days'
            POSTGRES_DROP_ls_prefixes_log: '2 days'
            POSTGRES_DROP_stats_chg_byprefix: '1 day'
            POSTGRES_DROP_stats_chg_byasn: '1 day'
            POSTGRES_DROP_stats_chg_bypeer: '1 day'
            POSTGRES_DROP_stats_ip_origins: '1 day'
            POSTGRES_DROP_stats_peer_rib: '1 day'
            POSTGRES_DROP_stats_peer_update_counts: '1 day'


    whois:
        image: openbmp/whois:2.2.0
        restart: unless-stopped
        deploy:
            restart_policy:
                condition: any
            placement:
                constraints:
                    - node.role == manager
        healthcheck:
            test: ["CMD-SHELL", "cat", "< /dev/null >", "/dev/tcp/localhost/43"]
            interval: 30s
            timeout: 10s
            retries: 3
        sysctls:
            - net.ipv4.tcp_keepalive_intvl=30
            - net.ipv4.tcp_keepalive_probes=5
            - net.ipv4.tcp_keepalive_time=180
        volumes:
            - openbmp_data:/config
        depends_on:
            - postgres
        ports:
            - "4300:43"
        environment:
            POSTGRES_PASSWORD: openbmp
            POSTGRES_USER: openbmp
            POSTGRES_DB: openbmp
            POSTGRES_HOST: postgres
            POSTGRES_PORT: 5432

    zookeeper:
        image: confluentinc/cp-zookeeper:7.7.1
        restart: unless-stopped
        deploy:
            restart_policy:
                condition: any
            placement:
                constraints:
                    - node.role == manager
        healthcheck:
            test: ["CMD-SHELL", "nc -z localhost 2181 || exit -1"]
            interval: 30s
            timeout: 10s
            retries: 5
        volumes:
            - zookeeper_data:/var/lib/zookeeper
        environment:
            ZOOKEEPER_CLIENT_PORT: 2181
            ZOOKEEPER_TICK_TIME: 2000

    kafka:
        image: confluentinc/cp-kafka:7.7.1
        restart: unless-stopped
        deploy:
            restart_policy:
                condition: any
            placement:
                constraints:
                    - node.role == manager
        depends_on:
            - zookeeper
        volumes:
            - kafka_data:/var/lib/kafka/data
        environment:
            KAFKA_BROKER_ID: 1
            KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
            KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:29092
            # Change/add listeners based on your FQDN that the host and other containers can access.  You can use
            #    an IP address as well. By default, only within the compose/containers can Kafka be accesssed
            #    using port 29092. Outside access can be enabled, but you should use an FQDN listener.
            KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092
            KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT
            KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
            KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
            KAFKA_NUM_PARTITIONS: 8
            KAFKA_LOG_RETENTION_MINUTES: 90
            KAFKA_LOG_ROLL_MS: 3600000
            KAFKA_LOG_SEGMENT_BYTES: 1073741824
            KAFKA_MESSAGE_MAX_BYTES: 100000000
            KAFKA_LOG_CLEANER_THREADS: 2
    cloudflared:
        image: cloudflare/cloudflared:latest
        restart: unless-stopped
        deploy:
            placement:
                constraints:
                    - node.role == manager
        healthcheck:
            test: ["CMD", "cloudflared", "--version"]
            interval: 30s
            timeout: 10s
            retries: 3
            start_period: 10s
        depends_on:
            - web
        entrypoint: |
            cloudflared tunnel run --token ${CLOUDFLARE_TUNNEL_TOKEN}

# Define volumes
volumes:
    relay_rrc00_data:
    relay_rrc01_data:
    relay_rrc03_data:
    relay_rrc04_data:
    relay_rrc05_data:
    relay_rrc06_data:
    relay_rrc07_data:
    relay_rrc10_data:
    relay_rrc11_data:
    relay_rrc12_data:
    relay_rrc13_data:
    relay_rrc14_data:
    relay_rrc15_data:
    relay_rrc16_data:
    relay_rrc18_data:
    relay_rrc19_data:
    relay_rrc20_data:
    relay_rrc21_data:
    relay_rrc22_data:
    relay_rrc23_data:
    relay_rrc24_data:
    relay_rrc25_data:
    relay_rrc26_data:
    relay_route_views2_data:
    relay_route_views3_data:
    relay_route_views4_data:
    relay_route_views5_data:
    relay_route_views6_data:
    relay_route_views7_data:
    relay_route_views8_data:
    relay_amsix_data:
    relay_amsix_ams_data:
    relay_cix_atl_data:
    relay_bdix_data:
    relay_bknix_data:
    relay_chicago_data:
    relay_chile_data:
    relay_eqix_data:
    relay_flix_data:
    relay_gixa_data:
    relay_gorex_data:
    relay_interlan_otp_data:
    relay_iraq_ixp_bgw_data:
    relay_isc_data:
    relay_kinx_icn_data:
    relay_namex_fco_data:
    relay_kixp_data:
    relay_jinx_data:
    relay_linx_data:
    relay_mwix_data:
    relay_napafrica_data:
    relay_nwax_data:
    relay_pacwave_lax_data:
    relay_pit_scl_data:
    relay_pitmx_qro_data:
    relay_phiox_data:
    relay_telxatl_data:
    relay_wide_data:
    relay_sydney_data:
    relay_saopaulo_data:
    relay_route_views2_saopaulo_data:
    relay_sg_data:
    relay_perth_data:
    relay_peru_data:
    relay_sfmix_data:
    relay_siex_data:
    relay_soxrs_data:
    relay_rio_data:
    relay_fortaleza_data:
    relay_uaeix_data:
    relay_ny_data:
    postgres_data:
    timescale_data:
    openbmp_data:
    zookeeper_data:
    kafka_data:
    web_data: